const TodayRoutine = props => {
  const { className, alt, src, onChange, value } = props;

  return (
    <div className="todayRoutine">
      <div></div>
    </div>
  );
};

export default TodayRoutine;
